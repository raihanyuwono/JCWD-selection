const sAuth = require("./auth");
const sUser = require("./user");
const sAttendance = require("./attendance");
const sPayroll = require("./payroll");

module.exports = {
    sAuth,
    sUser,
    sAttendance,
    sPayroll,
};
