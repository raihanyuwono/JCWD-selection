const rAuth = require("./rAuth");
const rUser = require("./rUser");
const rAttendance = require("./rAttendance");
const rPayroll = require("./rPayroll");

module.exports = {
    rAuth,
    rUser,
    rAttendance,
    rPayroll,
};
